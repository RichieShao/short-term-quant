# 系统体检与优化建议（2026-09-11）

> 体检范围：定时任务健康 / 数据新鲜度 / 情绪与周期引擎 / Lab 实验室 / 核心池追踪(coretrack) / 滚动校准(calib) / 发布链路
> 数据取自线上 `daily` 库、`api latest/health/coretrack/calib`，非本地估算

## 一、结论摘要

**流水线健康、数据实时、周期判断正确**；发现 **1 个线上可用性故障（P0）+ 3 个待优化点（P1–P3）+ 1 个尚未满足的复算条件**。

| 类别 | 项 | 状态 |
|---|---|---|
| 正常 | 每日定时任务 / 数据新鲜度 / 冰点反转模块 / Lab 引擎 | ✅ |
| **P0** | 发布镜像链接 **403 失效** | 🔴 需处理 |
| **P1** | 复盘页「历史回放·周期分层」数据丢失 | 🟠 |
| **P2** | 滚动校准卡与盘面页周期阶段不一致 | 🟡 |
| **P3** | 退潮期 Lab 分整体塌陷易被误读 | 🟡 |
| 暂缓 | Lab 权重复算条件未到（≥30 交易日） | ⏳ |

## 二、健康的（无需动）

- **定时任务**：09-09 / 09-10 / 09-11 每日两跑（16:05 主 + 16:40 兜底）全部 `ok:true`、无 error，单次耗时 14–17.5s（远低于 60s 预算）。`core_track` 偶为 `no-input`（当日无待结算核心池，属正常）。
- **数据新鲜度**：`latest = 2026-09-11`，`generated_at = 2026-09-11 16:40:07`。
- **周期降级链完整**（04→11）：`09-07 高潮 heat 67.6/涨停 93` → `09-08 59.1` → `09-09 43.2` → `09-10 29.8` → `09-11 22.4 冰点-退潮(涨停 40 / 跌停 21)`。
- **冰点反转模块工作正常**：`active=true / triggered=false / premium_ok=true / fb_ok=false`（今日首板 33 家 vs 5日均 45.4），提示「等信号，别抢跑：首板未放量，两者同时出现才动手」——与「退潮期空仓、等冰点反转」的纪律完全吻合。
- **Lab 引擎**：今日 `stage=退潮`、`pool_n=40`、`veto_n=0`、`ranks_n=40`、无截断、计算 7.3s。

## 三、问题清单（按优先级）

### P0 · 发布镜像链接已失效（403）🔴

- 09-09 发布的新域 `https://3000-d08f235a2bf04fb1a1ee911fb024c0ea.e2b.bj6.sandbox.cloudstudio.club/`，本日实测
  **首页 `403` / `/quant-api` `403`**（发布当时均为 200）。
- 旧域 `https://d08f235a2bf04fb1a1ee911fb024c0ea.app.workbuddy.link` 早已 **404**。
- **影响**：手机上现在打不开。
- **说明**：这是发布实例生命周期问题（休眠/回收），且**每次重发域名可能再变**。
- **可选方案**
  1. 重新发布恢复（最快，但域名可能再变，属于外部发布动作，**需你点头**）
  2. 加一个 CloudBase 定时函数定期 ping 保活（若属休眠则有效，若属回收则无效）
  3. 改用你网络可达的其他托管方式

### P1 · 复盘页「历史回放·周期分层」已变空 🟠

- `coretrack` 返回 `replay = null`（当前 `settled_days=6`、`last=2026-09-10`、`overall n=61 / adv 31% / avg +2.07%`）。
- 前端 `Cores.vue:108` 取 `ct.replay`，为 null 时 `rpStageRows/rpGradeRows` 返回空数组 → **该卡无内容**。
- 09-07 曾有过 141 样本回放数据，现已消失；`daily_job` 内未见 replay 相关写入逻辑，推测每日结算覆盖 `agg` 时丢失。
- **建议**：把回放重算并入每日 `core_track` 子步骤，或在写回 `agg` 时保留/复用旧 `replay` 字段。

### P2 · 滚动校准卡与盘面页「周期阶段」不一致 🟡

- 滚动校准（离线回放口径）：`last_stage=震荡`、`last_heat=36`（窗口 2025-08-18→2026-09-11，261 日）
- 线上快照（在线口径）：`cycle.stage=退潮`、`heat=22.4`、`band=冰点-退潮`
- **同一天（09-11）两个口径给出不同结论**，用户会困惑。
- **已知根因**：离线回放缺少炸板/跌停/溢价数据，而今日跌停 21 家使在线分显著低于离线（历史上离线一般高 5–8 分，跌停多时差距被放大）。
- **建议**：给该卡加「离线口径」标注；长期改为直接统计已入库的在线 `series`。

### P3 · 退潮期 Lab 分整体塌陷，易被误读 🟡

- 今日 `stage=退潮`，引擎对 E 统一 **−16** → 榜首仅 **61 分**（09-07 高潮时榜首 70）。
- 且 `退潮` 在 261 个交易日里仅出现 **3 天**，即 **−16 这个惩罚量级从未被充分校准过**（属启发式）。
- **风险**：把低分误读成「便宜机会」。按框架，退潮期本就应空仓，此时榜的意义是观察而非下单。
- **建议**：Lab 卡在 `stage=退潮/冰点` 时，顶部加一条提示「退潮期整体不建议参与，榜单仅供参考」。

## 四、尚未满足的复算条件（现在不要做）

- Lab 权重与惩罚刻度的复算目标是 **≥30 个交易日**样本。
- 现状：`coretrack settled_days=6`（n=61）；**含 Lab 的历史快照只有 5 天**（Lab 自 09-07 部署起才入库，早期补灌快照 lab=None）。
- 按当前节奏约需 **24 个交易日 ≈ 5 周（约 10 月中）**才达标。
- 在此之前复算 = 噪声拟合，会把 0.30/0.70 调成一堆过拟合参数。

## 五、建议执行顺序

1. **重发镜像**恢复访问（需你确认后再动）
2. **修 `coretrack.replay` 丢失**（恢复复盘页该卡）
3. **Lab 卡加退潮提示条**（小改动，`Check.vue`）
4. **校准卡加离线口径标注**（小改动，`Backtest.vue`）
5. **5 周后再做 Lab 权重复算**

## 六、顺带发现

- 项目根目录新增 `tutorial.html` / `使用教程.html`（非本系统已知产物）。若属正式交付物建议挂进页面导航，否则建议归档，避免根目录堆积。
- 261 日阶段分布：`震荡 127 / 高潮 80 / 发酵 34 / 启动 17 / 退潮 3` —— **退潮极稀疏**，任何以退潮为条件的规则都缺乏样本，结论应标注为启发式。
